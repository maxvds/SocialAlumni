extends layout

block content
  title Home
  link(href="/stylesheets/index.css" rel="stylesheet")
  //So the title is stored under the index.js page in the get home page router method 
  header(class="p-3 d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom" style="box-shadow: 0px 0px 8px #0007;")
      div(class="container")
        include nav 
  
  .welcome-message
    h1(class="welcome-title") Welcome to #{title}
    p(class="welcome-description") Networking for Otago Students and Alumni
